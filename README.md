# Prerequisites

Start with modifying `secrets.yaml` to something reasonable

# Compile

```
esphome compile ./thebox.yaml
```

# Compile and run

```
esphome compile ./thebox.yaml --device IP_ADDR
```
